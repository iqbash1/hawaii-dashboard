{
  "name": "hawaii-dashboard-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke tests for the Hawaii Dashboard",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:unit": "node --test utils.test.js compute.test.js qotd.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "http-server": "^14.1.1"
  }
}
